#!/usr/bin/env node
/*#
eval(compiler) {
    const files = compiler.matchFiles('**\/*', {
        excludeSelf:true
    })
    files.map(f => compiler.generate(`export * from ${f.relative()}`))
}
 */

console.log('what');
